org.gradle.jvmargs=-Xmx3G -XX:MaxMetaspaceSize=512m
org.gradle.parallel=true

# Mod metadata (shared across all mc* subprojects)
mod_version=2.2.0
maven_group=dev.sterner
archives_base_name=guardvillagers
